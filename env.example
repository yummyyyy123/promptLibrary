# Environment Variables Template
# Copy this to .env.local and fill in actual values
# NEVER commit actual values to version control

# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_key_here

# JWT Configuration
JWT_SECRET=your_jwt_secret_minimum_32_characters_long_random_string

# Admin Configuration
ADMIN_USERNAME=your_admin_username
ADMIN_PASSWORD=your_admin_password_minimum_8_characters

# Application Configuration
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Optional: Database URL (if using external database)
# DATABASE_URL=postgresql://user:password@localhost:5432/database

# Optional: Third-party API Keys (store in Supabase Vault instead)
# OPENAI_API_KEY=your_openai_api_key
# TWILIO_API_KEY=your_twilio_api_key

# Security Notes:
# 1. Never commit actual values to git
# 2. Use strong, unique secrets (minimum 32 characters for JWT_SECRET)
# 3. Use different secrets for development and production
# 4. Rotate secrets regularly
# 5. Store third-party API keys in Supabase Vault when possible
